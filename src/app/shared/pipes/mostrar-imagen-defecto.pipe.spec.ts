import { MostrarImagenDefectoPipe } from './mostrar-imagen-defecto.pipe';

describe('MostrarImagenDefectoPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarImagenDefectoPipe();
    expect(pipe).toBeTruthy();
  });
});
